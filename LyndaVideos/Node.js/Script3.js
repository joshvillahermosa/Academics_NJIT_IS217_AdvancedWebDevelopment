var rl = require('readline');

var prompts = rl.createInterface(process.stdin, proces.dtout);

prompts.question("How many serving of fruit and vegitbles do you eat?", function(servings)){
	function (servings){
		var message = '';
		if (servings < 5){
			
		}
	}
}